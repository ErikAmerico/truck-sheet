generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model employee {
  id        Int    @id @default(autoincrement())
  role      String
  username  String @unique
  password  String
  firstName String
  lastName  String
}
